CREATE TABLE  AGENCIAS 
   (	CD_AGENCIA numeric(4,0), 
	NM_AGENCIA VARCHAR(50), 
	CD_BANCO numeric(3,0), 
	 CONSTRAINT AGENCIAS_PK PRIMARY KEY (CD_AGENCIA), 
	 CONSTRAINT AGENCIAS_UK1 UNIQUE (NM_AGENCIA), 
	 CONSTRAINT AGENCIAS_FK FOREIGN KEY (CD_BANCO)
	  REFERENCES  BANCOS (CD_BANCO)
   )